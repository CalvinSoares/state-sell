# Banco (Neon Postgres)
DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"

# E-mail (Resend)
RESEND_API_KEY=""
# live = envia de verdade | dry = só renderiza no log. Ver arquitetura/visao-geral.md
RESEND_MODE="dry"

# Segurança
CRON_SECRET=""      # Authorization: Bearer nas rotas /api/cron/*
AUTH_SECRET=""      # assinatura do magic link

# App
APP_URL="http://localhost:3000"

# Backoffice — allowlist de e-mails separados por vírgula. Ver backoffice.md
ADMIN_EMAILS=""
